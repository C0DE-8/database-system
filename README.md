# database-system

Use `PROJECT_DBMS_TEMPLATE.txt` when you want to add an old or new project to this DBMS.

Fill in the template and send it back. The app should be wired using `test/shop-backend` as the example pattern: `SITE_ID`, `API_KEY`, `DBMS_URL`, `diamond-sql`, and `connectProject(...)`.
